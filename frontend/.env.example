# Base URL of the backend API — no trailing slash.
#
# Local development:  http://localhost:5000
# Production:         https://api.ntiolympiad.in
#
# This is read at BUILD time, not at runtime. Changing it requires a rebuild.
VITE_API_URL="http://localhost:5000"

# Public site origin, used for canonical URLs and social preview tags.
VITE_SITE_URL="http://localhost:5173"
