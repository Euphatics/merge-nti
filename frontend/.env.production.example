# Copy to .env.production before running `npm run build` for a release.
# .env.production is gitignored — these values are baked into the bundle.

VITE_API_URL="https://api.ntiolympiad.in"
VITE_SITE_URL="https://ntiolympiad.in"
